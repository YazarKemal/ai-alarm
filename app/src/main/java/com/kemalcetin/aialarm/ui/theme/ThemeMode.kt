package com.kemalcetin.aialarm.ui.theme

/** User-facing theme choice. Persisted in DataStore. */
enum class ThemeMode(val label: String) {
    SYSTEM("System"),
    LIGHT("Light"),
    DARK("Dark")
}
